# Run Tests

Run the full test suite and report results.

1. Run `uv run pytest -v --tb=short`
2. If tests fail, identify the root cause and fix it
3. Re-run until all tests pass
4. Report: total passed, failed, skipped, and any warnings
